local a,b=Shared.UseResource()if not a then return end;Client.Keys=Client.Keys or{}Client.Keys.Give=function(c,d)local e=Client.GetPlate(c,d)if not e then return print("Vehicle does not have a plate")end;TriggerServerEvent('qb-vehiclekeys:server:AcquireVehicleKeys',e)end;Client.Keys.Remove=function(c,d)return print("Can only remove keys on the server")end;Client.Keys.Has=function(c,d)local e=DoesEntityExist(d)and Client.GetPlate(c,d)if not e then return false end;return b:HasKeys(e)end